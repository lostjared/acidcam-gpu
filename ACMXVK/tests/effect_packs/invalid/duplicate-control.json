{
    "format": "acmxvk-effect-pack",
    "version": 1,
    "id": "invalid.duplicate-control",
    "name": "Invalid Duplicate Control",
    "passes": ["shader.frag"],
    "controls": [
        {"id": "amount", "label": "Amount", "uniform": "amount", "minimum": 0, "maximum": 1, "step": 0.1, "default": 0.5},
        {"id": "amount", "label": "Other", "uniform": "other", "minimum": 0, "maximum": 1, "step": 0.1, "default": 0.5}
    ]
}
